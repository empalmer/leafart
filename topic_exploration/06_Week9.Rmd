---
title: "Week 9"
author: "Emily Palmer"
date: "11/23/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
devtools::load_all()
```

## Test functions are working 

### Test the movable leaves work 
```{r}
# One leaf
get_gingko_params() %>% 
  full_leaf_origin() %>% 
  rake_leaves() %>% 
  plot_leaves()

# many leaves 


```

### Test the initially randomly placed and rotated leaves work

```{r}
# one leaf
get_gingko_params() %>%
  grow_leaf_random() %>% 
  rake_leaves() %>%
  plot_leaves()

# multiple leaves
get_gingko_params() %>% 
  make_the_leaves_fall() %>% 
  plot_leaves()


#grow_leaf_layers_random(initialize, param)
#one_branch_random(partial_leaf = initialize, param = param)
```




## Play with different parameter values and output plots 


```{r}

```

